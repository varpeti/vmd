# Titles

# Big title (h1)
## Middle title (h2)
### Smaller title (h3)
#### and so on (h4)
##### and so on (h5)
###### and so on (h6)

# Paragraphs

test line
test line2
test line3

another testline

# Formating

**blod**
This //text// is //italic//.
--underline--
~~deleted~~
2^^x^^+3
H__2__O
This #00ff00 apple# is green.
This is an @dddddd important@ word.

# Lines

single
--
double
==

# Math

## @ mode have some awsome shortcuts
functionname(sub;sup;x)
$$ int_{0}^{oo} (alpha*beta)
@@ int(0; oo; alpha*beta )
@@ int(-oo;-oo; sum(t=-pi/2;pi/2;x^(i*t)) )

## Inline math
Many symbols can also be displayed using a TeX alternative, $$@ pm sqrt( sum(i=-oo; oo; Delta i) )*sqrt(5)$ but a preceeding backslash is not required.

There is another example: @@lim[x->oo] lim[y->oo] (1/(1/x + 1/y))@ not math: (pi/2), math: $$lim_{x->oo} lim_{y->oo} (1/(1/x + 1/y))$ again, not math.

## @ mode
@@ lim_{x->oo} lim_{y->oo} (1/(1/x + 1/y))
@@ lim[x->oo] lim[y->oo] (1/(1/x + 1/y))
$$ lim_{x->oo} lim_{y->oo} (1/(1/x + 1/y))

## Matrixes are only in normal mode
$$ [[a,b,c],[d,e,f]]


# Code

```c++
#include <iostream>

int main(int argc, char *argv[]) {

  /* An annoying "Hello World" example */
  for (auto i = 0; i < 0xFFFF; i++)
    cout << "Hello, World!" << endl;

  char c = '\n';
  unordered_map <string, vector<string> > m;
  m["key"] = "\\\\"; // this is an error

  return -2e3 + 12l;
}
```

# Table

```
Name    Age
aaa     23
bbb     34
ccc     242
ddd     934 
```

## Link

This is a link: link=https://www.w3schools.com/html/html_links.asp;w3link;

This is a jump: jump=#Titles;Top;

# Picture

This is a picture: img=https://www.w3schools.com/html/pulpitrock.jpg;w3pic;

## Table of Contents

:Table of Contents:30: